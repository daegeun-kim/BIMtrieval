# Base URL of the local BIM_RAG FastAPI backend. No secrets, keys, DSNs, or
# filesystem paths belong in this file — the frontend only ever talks to this
# HTTP API (spec_v006 §10.5, §17).
VITE_API_BASE_URL=http://localhost:8000
